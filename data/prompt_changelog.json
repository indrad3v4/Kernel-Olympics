{
  "versions": [
    {
      "version": "v4.0.0",
      "date": "2026-07-10",
      "author": "fable",
      "changes": [
        "Kimi code prompt: resolve the collision between 'keep main()' and 'drop the unresolved-header code path' — main() calls that path, so reproduce it minus the dropped call",
        "Truncation marker was an HTML comment (<!-- ... -->) spliced into C++ source: at file scope that is 'expected external declaration' at col 1 and 'expected unqualified-id' at col 3. Now a // comment; truncation detected from the raw model output",
        "_sanitize_extracted strips leaked closing fences, trailing prose and the truncation marker from every _extract_code path",
        "Markdown fence language tag generalized — ```c++ fell through the (cuda|hip|cpp|python) allow-list and swallowed the closing fence plus prose",
        "_postprocess_port restores main() BEFORE the mechanical pass, so the helper-shim injection (gated on the file having a main()) actually fires for the restored driver",
        "Restore is dependency-aware (_unsatisfied_main_calls): a driver calling a helper the port does not define is not reattached — that trades a compile error for an unfixable link error",
        "Helper-shim re-injection guard keys on the shim's definitions, not its marker comment, which _extract_code strips",
        "Helper shim now defines EXIT_WAIVED (a helper_string.h macro that _fix_ported_code strips along with its header)"
      ],
      "files_changed": [
        "src/router.py",
        "tests/test_main_preservation.py"
      ],
      "run_results": {}
    },
    {
      "version": "v3.0.0",
      "date": "2026-07-10",
      "author": "fable",
      "changes": [
        "DeepSeek system prompt: when shown a mechanically translated HIP draft, plan only the wavefront64 semantics delta as a terse checklist",
        "DeepSeek plan prompt: hipified_source switches it to a delta prompt with no CUDA original embedded (15555 -> 4965 chars on nvidia_shfl_scan.cu), capped at PLAN_DELTA_MAX_TOKENS=640",
        "GLM error-analyst prompt: self_contained flag stops it reporting a truncated-away main() as a defect, and pins the fix to 'restore main() from the original source'",
        "main() preservation: _extract_main / _ensure_main_preserved / _postprocess_port reattach a dropped driver before the first compile, with no LLM call",
        "Linker-only short-circuit: _is_linker_only skips the GLM analyst and both DeepSeek re-plans when every diagnostic is a link failure",
        "Budget-aware refine dispatch: a refine (and its retry) is skipped unless the clock holds the call plus COMPILE_RESERVE_SECONDS"
      ],
      "files_changed": [
        "src/router.py",
        "tests/test_main_preservation.py"
      ],
      "run_results": {}
    },
    {
      "version": "v2.0.0",
      "date": "2026-07-10",
      "author": "fable",
      "changes": [
        "Kimi code prompt: edit a mechanically hipified draft instead of re-porting from scratch",
        "Kimi code prompt: checklist narrowed from 9 items to the 4 wavefront64 semantics a regex cannot do",
        "Kimi refine prompt: state that the mechanical pass is applied; name cuda* symbols that crept back",
        "Output-token budget sized to the kernel via _compute_adaptive_max_tokens instead of a flat 16384"
      ],
      "files_changed": [
        "src/router.py"
      ],
      "run_results": {}
    },
    {
      "version": "v1.0.0",
      "date": "2026-07-09",
      "author": "fable",
      "changes": [
        "Initial versioned system prompts for DeepSeek (planner), Kimi (coder), GLM (evaluator)",
        "Moved the GLM error-analyst role from an inline call-site string into SYSTEM_PROMPTS",
        "Added the two-layer SIGSEGV constraint to the Kimi refine prompt (frozen Layer 1 base)",
        "Refine prompt now reports the real stagnation threshold instead of a hardcoded 3"
      ],
      "files_changed": [
        "src/router.py"
      ],
      "run_results": {}
    }
  ],
  "current": "v4.0.0"
}
