flask>=3.0
numpy>=1.24
requests>=2.31
chromadb>=0.4  # optional: for production vector store

# AMD ROCm / HIP deps (on AMD GPU instance)
# hipcc, rocm-dev, rocm-libs — installed via Docker

# Development
pytest>=7.0
pytest-cov>=4.0
