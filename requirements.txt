# Kernel Olympics has no third-party *runtime* dependencies — the pipeline
# runs on the Python standard library (urllib for HTTP, sqlite3 for the
# pattern store). The packages below are for development / CI only.

# AMD ROCm / HIP deps (installed via Docker image, not pip)
#   - hipcc, rocm-dev, rocm-libs

# Development
pytest>=7.0      # Unit test runner
pytest-cov>=4.0  # Coverage reports
