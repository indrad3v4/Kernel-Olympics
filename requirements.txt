flask>=3.0       # Optional: web UI for demo dashboard
numpy>=1.24      # Numerical computation in reference outputs
requests>=2.31   # HTTP client for Fireworks / DeepSeek API calls
chromadb>=0.4    # Optional: production-grade vector store (SQLite default)

# AMD ROCm / HIP deps (installed via Docker image, not pip)
#   - hipcc, rocm-dev, rocm-libs

# Development
pytest>=7.0      # Unit test runner
pytest-cov>=4.0  # Coverage reports
