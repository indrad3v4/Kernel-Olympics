# Kernel Olympics — Web + Development dependencies
# The core pipeline runs on the Python standard library alone.
# These packages add the web frontend (FastAPI) and testing tools.

# Web frontend (deployed via Railway / Docker)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Development / CI
pytest>=7.0
pytest-cov>=4.0
